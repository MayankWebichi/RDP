name: Max RDP Optimization (Fast Boot)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Display System Specifications
        run: |
          Write-Host "--- SYSTEM SPECIFICATIONS ---"
          $cpu = Get-CimInstance Win32_Processor | Select-Object Name, NumberOfCores
          $ram = Get-CimInstance Win32_PhysicalMemory | Measure-Object Capacity -Sum | Select-Object @{Name="TotalGB";Expression={$_.Sum / 1GB}}
          Write-Host "CPU: $($cpu.Name) ($($cpu.NumberOfCores) Cores)"
          Write-Host "RAM: $($ram.TotalGB) GB"
          Get-PSDrive C | Select-Object Used, Free | Format-Table -AutoSize

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall set rule group="Remote Desktop" new enable=Yes
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $password = "MaxRDP_Pass_2024!" 
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          echo "RDP_PASSWORD=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        env:
          TAILSCALE_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$env:TAILSCALE_KEY --hostname=gh-max-rdp
          $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Connection Summary
        run: |
          Write-Host "`n=================================="
          Write-Host "   MAX RDP CONNECTION INFO"
          Write-Host "=================================="
          Write-Host "IP: $env:TAILSCALE_IP"
          Write-Host "User: RDP"
          Write-Host "Pass: MaxRDP_Pass_2024!"
          Write-Host "==================================`n"

      - name: Max Uptime Loop
        run: |
          $start = Get-Date
          while ($true) {
              $current = Get-Date
              $elapsed = ($current - $start).TotalMinutes
              if ($elapsed -ge 350) { break }
              Write-Host "[$(Get-Date)] RDP Active - Up for $([math]::Round($elapsed, 0)) mins"
              Start-Sleep -Seconds 300
          }
